# ============================================================
# Institutional Memory System — Requirements
# Last verified: July 2025
# Python >= 3.11 required
# ============================================================

# ── Core LangChain Ecosystem ────────────────────────────────
langchain>=1.3.11
langchain-community>=0.3.23
langchain-google-genai>=4.2.6        # Uses new consolidated google-genai SDK
langchain-text-splitters>=0.3.8
langgraph>=1.2.7
langgraph-prebuilt>=0.5.2

# ── Google Gemini SDK ───────────────────────────────────────
google-genai>=1.16.0                 # New consolidated SDK (replaces google-generativeai)

# ── Configuration & Validation ──────────────────────────────
pydantic>=2.11.5
pydantic-settings>=2.14.2

# ── Vector Database ─────────────────────────────────────────
chromadb>=1.5.9

# ── Embeddings ──────────────────────────────────────────────
sentence-transformers>=4.1.0

# ── Graph Database ──────────────────────────────────────────
neo4j>=5.28.1

# ── Data Processing ─────────────────────────────────────────
pandas>=2.2.3
numpy>=2.2.6
python-dotenv>=1.1.0
tiktoken>=0.9.0

# ── Document Processing ─────────────────────────────────────
pypdf>=5.5.0
unstructured>=0.17.2

# ── API Backend ─────────────────────────────────────────────
fastapi>=0.115.12
uvicorn[standard]>=0.34.3
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.20

# ── Frontend ────────────────────────────────────────────────
streamlit>=1.45.1

# ── Logging ─────────────────────────────────────────────────
loguru>=0.7.3

# ── Utilities ───────────────────────────────────────────────
tqdm>=4.67.1

# ── Testing ─────────────────────────────────────────────────
pytest>=8.3.5
pytest-asyncio>=0.26.0
pytest-mock>=3.14.0
httpx>=0.28.1

# ── Optional: Audio Transcription ───────────────────────────
# Uncomment only when building meeting transcription agent
# openai-whisper>=20231117          # Also requires: pip install ffmpeg

pandas
